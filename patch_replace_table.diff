--- src/pages/SettingPage.tsx
+++ src/pages/SettingPage.tsx
@@ -93,6 +93,18 @@
     }
   };
 
+  const handleUpdateBulkMapping = async (ids: number[], customName: string | null, mainGroup: string, subGroup: string | null) => {
+    for (const id of ids) {
+      await invoke('update_segment_mapping', {
+        id,
+        customName, // only applied if customName is provided (single select)
+        mainGroup,
+        subGroup
+      });
+    }
+    handleLoadMappings();
+  };
+
   const handleUpdateMapping = async (id: number, customName: string | null, mainGroup: string, subGroup: string | null) => {
     try {
       await invoke('update_segment_mapping', {
@@ -269,67 +281,10 @@
                   </button>
                 </div>
 
-                <div className="bg-bg-surface border border-border rounded-xl overflow-hidden">
-                  <div className="overflow-x-auto">
-                    <table className="w-full text-left border-collapse">
-                      <thead>
-                        <tr className="bg-bg-base border-b border-border">
-                          <th className="p-3 text-xs font-bold text-text-secondary uppercase tracking-wider">Ch / Code</th>
-                          <th className="p-3 text-xs font-bold text-text-secondary uppercase tracking-wider">AreaTable</th>
-                          <th className="p-3 text-xs font-bold text-text-secondary uppercase tracking-wider">Custom Name</th>
-                          <th className="p-3 text-xs font-bold text-text-secondary uppercase tracking-wider">Main Group</th>
-                          <th className="p-3 text-xs font-bold text-text-secondary uppercase tracking-wider">Sub Group</th>
-                          <th className="p-3 text-xs font-bold text-text-secondary uppercase tracking-wider text-center">Action</th>
-                        </tr>
-                      </thead>
-                      <tbody className="divide-y divide-border">
-                        {mappings.map(map => (
-                          <tr key={map.id} className="hover:bg-bg-base/50 transition-colors">
-                            <td className="p-3 text-sm font-mono text-text-secondary">CH{map.dts_ch}-{map.dts_code}</td>
-                            <td className="p-3 text-sm font-mono text-text-primary">{map.original_name}</td>
-                            <td className="p-3">
-                              <input 
-                                type="text" 
-                                defaultValue={map.custom_name || ''} 
-                                onBlur={(e) => handleUpdateMapping(map.id, e.target.value || null, map.main_group, map.sub_group)}
-                                placeholder="Alias (e.g. Area 1)"
-                                className="w-full bg-bg-base border border-border rounded p-2 text-sm text-text-primary focus:border-scada-primary focus:outline-none"
-                              />
-                            </td>
-                            <td className="p-3">
-                              <select 
-                                value={map.main_group}
-                                onChange={(e) => handleUpdateMapping(map.id, map.custom_name, e.target.value, map.sub_group)}
-                                className="w-full bg-bg-base border border-border rounded p-2 text-sm text-text-primary focus:border-scada-primary focus:outline-none"
-                              >
-                                <option value="Unassigned">Unassigned</option>
-                                <option value="BC4">BC4</option>
-                                <option value="BC5">BC5</option>
-                                <option value="Tunnel TCM">Tunnel TCM</option>
-                                <option value="Tunnel BEK">Tunnel BEK</option>
-                              </select>
-                            </td>
-                            <td className="p-3">
-                              <input 
-                                type="text" 
-                                defaultValue={map.sub_group || ''} 
-                                onBlur={(e) => handleUpdateMapping(map.id, map.custom_name, map.main_group, e.target.value || null)}
-                                placeholder="Sub (e.g. TCM 1)"
-                                className="w-full bg-bg-base border border-border rounded p-2 text-sm text-text-primary focus:border-scada-primary focus:outline-none"
-                              />
-                            </td>
-                            <td className="p-3 text-center">
-                              <button onClick={() => alert('Saved!')} className="text-scada-primary hover:text-white transition-colors">
-                                <Save size={16} />
-                              </button>
-                            </td>
-                          </tr>
-                        ))}
-                        {mappings.length === 0 && (
-                          <tr>
-                            <td colSpan={6} className="p-6 text-center text-text-secondary text-sm">
-                              No segments found. Please click "SYNC FROM DTS" to fetch AreaTables.
-                            </td>
-                          </tr>
-                        )}
-                      </tbody>
-                    </table>
-                  </div>
-                </div>
+                {mappings.length > 0 ? (
+                  <MappingGrid mappings={mappings} onUpdateBulk={handleUpdateBulkMapping} />
+                ) : (
+                  <div className="bg-bg-surface border border-border rounded-xl p-8 text-center text-text-secondary text-sm">
+                    No segments found. Please click "SYNC FROM DTS" to fetch AreaTables.
+                  </div>
+                )}
               </div>
             )}
