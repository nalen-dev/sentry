--- SettingPage.tsx
+++ SettingPage.tsx
@@ -4,6 +4,7 @@
 import { useToast } from '../contexts/ToastContext';
 import { DUMMY_AREAS } from '../data/constants';
 import MappingGrid from '../components/MappingGrid';
+import ConfirmModal from '../components/ConfirmModal';
 
 interface SegmentMapping {
   id: number;
@@ -48,6 +49,7 @@
   
   const [saving, setSaving] = useState(false);
   const [syncing, setSyncing] = useState(false);
+  const [showSyncConfirm, setShowSyncConfirm] = useState(false);
 
   useEffect(() => {
     loadMappings();
@@ -79,15 +81,10 @@
   };
 
   const handleSyncDTS = async () => {
-    if (window.confirm("WARNING: This will replace your local segment groupings with the latest configuration from the DTS. Unassigned segments will be placed in the 'Unassigned' group.\n\nAre you sure you want to proceed?")) {
-      try {
-        setSyncing(true);
-        await invoke('sync_dts_segments');
-        await loadMappings();
-        addToast({ title: 'Sync Successful', message: 'DTS active channels imported.', type: 'success' });
-      } catch (err: any) {
-        addToast({ title: 'Sync Failed', message: err.toString(), type: 'error' });
-      } finally {
-        setSyncing(false);
-      }
-    }
+    setShowSyncConfirm(true);
+  };
+
+  const confirmSyncDTS = async () => {
+    try {
+      setSyncing(true);
+      await invoke('sync_dts_segments');
