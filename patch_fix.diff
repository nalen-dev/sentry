--- src/pages/SettingPage.tsx
+++ src/pages/SettingPage.tsx
@@ -108,18 +108,8 @@
         subGroup
       });
     }
-    handleLoadMappings();
-  };
-
-  const handleUpdateMapping = async (id: number, customName: string | null, mainGroup: string, subGroup: string | null) => {
-    try {
-      await invoke('update_segment_mapping', { id, customName, mainGroup, subGroup });
-      // reload mappings
-      const fetchedMappings: SegmentMapping[] = await invoke('get_segment_mappings');
-      setMappings(fetchedMappings);
-    } catch (err) {
-      console.error("Failed to update mapping:", err);
-    }
+    const fetchedMappings: SegmentMapping[] = await invoke('get_segment_mappings');
+    setMappings(fetchedMappings);
   };
 
   const handleTestConnection = async () => {
